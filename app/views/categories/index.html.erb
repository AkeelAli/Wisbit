<h1>Listing categories</h1>

<% if not params[:option] %>
	TODO: Redirect to main page from controller
<% else %>

	<table>
  	<tr>
    	<th>Name</th>
    	<th></th>
    	<th></th>
    	<th></th>
  	</tr>

	<% @categories.each do |category| %>
  	<tr>
    	<td><%= link_to category.name, params[:option]=="mash"?mash_index_path(:category=>category.id, :index=>0, :win=>0):read_index_path(:category=>category.id, :index=>0)  %></td>
		</tr>
	<% end %>
	</table>

	<br />

	<%= link_to 'New Category', new_category_path %>

<% end %>
